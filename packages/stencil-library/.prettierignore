# Build artifacts
www/
dist/
loader/
hydrate/
node_modules/
.stencil/
storybook-static/

# Generated by `stencil build --docs`.
# Stencil emits a trailing space after @public on props that declare a
# @default, which fights the trailing-whitespace pre-commit hook and causes
# the file to flip back and forth on every build. Leave it untouched.
src/components.d.ts
