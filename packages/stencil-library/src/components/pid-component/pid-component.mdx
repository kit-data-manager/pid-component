import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';

import * as PIDComponentStories from './pid-component.stories';

<Meta of={PIDComponentStories} />

# PID Component

The `pid-component` dynamically renders a component based on the value of the `value` property.
It automatically detects the format of the input `value` and renders the appropriate sub-component.
Depending on the value, it decides which component to render, what priority to give it, and what props to pass to it.

It also renders itself recursively for all its children when unfolded.
You can set the maximum depth of recursion with the `level-of-subcomponents` property.
By default, it is set to 1, which means that it will only render the first level of children, but not their children.
You can prohibit unfolding of the component by setting the `current-level-of-subcomponents` to the same value as the `level-of-subcomponents` property.

You can find the source code of the component [on GitHub](https://github.com/kit-data-manager/pid-component).
To use the component, import the [npm-package](https://www.npmjs.com/package/@kit-data-manager/pid-component)
via [unpkg](https://unpkg.com/):

```html
<head>
  <script type="module">
    import { defineCustomElements } from 'https://cdn.jsdelivr.net/npm/@kit-data-manager/pid-component/dist/loader/index.js';
    defineCustomElements();
  </script>
</head>
```

**Please notice that you must use the hyphenated version of an attribute when using the component directly in HTML (e.g. `currentLevelOfSubcomponents` -> `current-level-of-subcomponents`).
When using inside Stencil or with JSX/TSX syntax, you must use the camelCase version.**

## Table of Contents

- [Default](#default)
- [Controls](#controls)
- [Display Mode](#display-mode)
- [Emphasis](#emphasis)
- [Pagination](#pagination)
- [Renderer Selection](#renderer-selection)
- [Supported Types](#supported-types)
- [Advanced Configuration](#advanced-configuration)

## Default

<Canvas of={PIDComponentStories.Default} />

## Controls

<Controls />

---

## Display Mode

### Collapsed by Default

The `openByDefault` property controls the initial state of the component.

<Canvas of={PIDComponentStories.OpenByDefaultCollapsed} />

### Expanded by Default

<Canvas of={PIDComponentStories.OpenByDefaultExpanded} />

---

## Emphasis

### Emphasized (Default)

The `emphasizeComponent` property adds a subtle border and shadow to make the component stand out.

<Canvas of={PIDComponentStories.Emphasized} />

### Unemphasized

Set `emphasizeComponent="false"` for inline use in flowing text.

<Canvas of={PIDComponentStories.Unemphasized} />

---

## Pagination

### Default Page Size (10 items)

The `itemsPerPage` property controls how many entries are shown per page.

<Canvas of={PIDComponentStories.DefaultPageSize} />

### Unlimited Page Size

Set `itemsPerPage` to a high value (e.g., 100) to show all entries on a single page.

<Canvas of={PIDComponentStories.UnlimitedPageSize} />

---

## Renderer Selection

The `renderers` property allows you to specify an ordered list of renderer types to use.
This is useful when you want to prioritize certain types or restrict detection to specific types.

### Matching Renderer

When a renderer in the list matches the value, it is used to render the component.

<Canvas of={PIDComponentStories.RenderersMatchingDOI} />

### Preselection with Fallback

When `renderers` is set but none match, the component falls back to the full registry (default behavior).

<Canvas of={PIDComponentStories.RenderersPreselectionFallback} />

### Strict Restriction

Set `fallbackToAll="false"` to strictly restrict rendering to only the listed renderers.

<Canvas of={PIDComponentStories.RenderersStrictRestriction} />

### Order Determines Priority

The order in the `renderers` array determines which renderer is used when multiple could match.

<Canvas of={PIDComponentStories.RenderersOrderPriority} />

### Correct Order

<Canvas of={PIDComponentStories.RenderersCorrectOrder} />

---

# Supported Types

The following types are supported, ordered by their detection priority (lower = higher priority) as defined in `utils.ts`:

- [Date](./?path=/docs/renderer-date--docs) (priority 0)
- [ORCiD](./?path=/docs/renderer-orcid--docs) (priority 1)
- [DOI](./?path=/docs/renderer-doi--docs) (priority 2)
- [Handle](./?path=/docs/renderer-handle--docs) (priority 3)
- [ROR](./?path=/docs/renderer-ror--docs) (priority 4)
- [SPDX](./?path=/docs/renderer-spdx--docs) (priority 5)
- [Email](./?path=/docs/renderer-email--docs) (priority 6)
- [URL](./?path=/docs/renderer-url--docs) (priority 7)
- [Locale](./?path=/docs/renderer-locale--docs) (priority 8)
- [JSON](./?path=/docs/renderer-json--docs) (priority 9)
- [Fallback](./?path=/docs/renderer-fallback--docs) (priority 99)

---

# Advanced Configuration

## Using Settings

You can pass a JSON array to the `settings` property to configure specific types.

```html
<pid-component
  value="..."
  settings='[
    {"type": "ORCIDType", "values": [{"name": "showAffiliation", "value": false}]},
    {"type": "DOIType", "values": [{"name": "citationStyle", "value": "IEEE"}]}
  ]'
></pid-component>
```

## Recursive Rendering

The component supports recursive rendering for complex data structures (like a PID record containing ORCiDs).

- `level-of-subcomponents`: Controls how deep the recursion goes (default: 1).
- `current-level-of-subcomponents`: Used internally to track depth. Set it equal to `level-of-subcomponents` to prevent initial unfolding.
- `hide-subcomponents`: Boolean attribute to completely disable sub-component rendering.

<Canvas of={PIDComponentStories.TypedPIDMakerExampleText} />
